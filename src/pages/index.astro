---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug"

const projects = (await getCollection("project")).sort((a, b) => b.data.year - a.data.year)

const last_projects = projects.slice(0, 3)
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Bonjour 👋</div>
    <div class="text-5xl font-bold">Je suis Matthieu Robert</div>
    <div class="text-3xl py-3 font-bold">Developpeur backend et DevOps</div>
    <div class="py-2">
      <!-- <text class="text-lg">
        je suis un étudiant de 22 ans en troisième année de BUT Informatique en alternance au sein de l'entreprise CSGroup.
Je suis passionné d’informatique depuis mon enfance et c’est ainsi que j’ai pu me former et découvrir de nombreuses technologies et langages comme le Go, Docker, Kubernetes ou la CI.
Durant ces années de découverte et d’apprentissage, j’ai pu faire quelques projets que vous pourrez découvrir ci-dessous
      </text> -->
    </div>
    <div class="mt-8">
      <a class="btn" href="https://github.com/matthieurobert" target="_blank"> Github</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">Mes derniers projets {"</>"}</div>
  </div>

  {
    last_projects.map((project) => (
      <>
        <HorizontalCard
          title={project.data.title}
          img={project.data.img}
          desc={project.data.description}
          url={project.data.url}
          target="_self"
          badge={project.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
